#include <iostream>

using namespace std;

int main()
{

    double temp;
    cout << "Enter a temperature: ";
    cin >> temp;

    std::cout << temp << " degrees Celsius is " << temp * 1.8 + 32  << " degrees Fahrenheit " << endl;
std::cout<< temp << " degrees Fahrenheit is " << (temp-32)/1.8 << " degrees Celsius " << endl;
    return 0;

    return 0;
}
